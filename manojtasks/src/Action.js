import { createSlice } from "@reduxjs/toolkit";

const totalActions = createSlice({
    name:"increament",
    initialState:{
        name:"venkatesh",
        age:"10",
        phone:"994855"
    },
    reducers:{
    }
})
export default totalActions.reducer;